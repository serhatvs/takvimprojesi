# Test Matrix

| Degisiklik alani | Calistirilacak kontroller |
| --- | --- |
| Kok workspace, package veya turbo ayarlari | Node 24 aktifken `pnpm install --frozen-lockfile --reporter=append-only`, `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm test:integration`, `NEXT_TELEMETRY_DISABLED=1 pnpm build` |
| `apps/web` | `pnpm --filter @agu/web lint`, `pnpm --filter @agu/web typecheck`, `pnpm --filter @agu/web test`, `pnpm --filter @agu/web build`; public helper testlerinde liste linkleri, detail linkleri, geri donus filtreleri, detail mapper, 404/API hata ayrimi, metadata ozeti, detay sayfasi registration panel state/helper davranislari, QR yoklama paneli yetki/payload/expiry/hata state davranislari, katilim ozeti paneli yetki/endpoint/options/metrik bicimlendirme/refresh/hata state davranislari, `/check-in` parser/kamera state/manuel yedek/check-in hata davranislari, `/club-dashboard` veri secimi/filtreleme/URL yapici helper testleri, `/club-dashboard/events/new` tarih donusum/kapasite/contract/safe error helper testleri ve `/press-dashboard` yetki/rol/kuyruk/review panelleri/safe error helper testleri |
| `apps/api/src` | `pnpm --filter @agu/api lint`, `pnpm --filter @agu/api typecheck`, `pnpm --filter @agu/api test`, `pnpm --filter @agu/api test:integration`, `pnpm --filter @agu/api build` |
| `apps/api/src/auth` | Unit: session olusturma/dogrulama, gecersiz ve expired token, global role guard, dev auth production/flag engeli. Integration: dev-login, bilinmeyen kullanici, cookie ile `/auth/me`, cookiesiz `/auth/me`, logout sonrasi 401, dev auth kapali login engeli, token'a gomulen sahte rolun yok sayilmasi |
| `apps/api/src/press` | Unit ve Integration: `GET /press/events` endpointi yalniz `PRESS_EDITOR` ve `SYSTEM_ADMIN` tarafindan erisilebilir, `STUDENT` veya `CLUB_ADMIN` 403 alir, yalniz `SUBMITTED` etkinlikler `updatedAt ASC` sirasiyla ve sayfalama/arama filtreleriyle doner. |
| `apps/api/src/events` | Unit: public list yalniz `PUBLISHED`, varsayilan gecmis dislama, `from/to`, `clubId`, `q`, pagination, siralama, public mapper veri minimizasyonu, public detail 200/404; yetkili kulup admini draft olusturur, yetkisiz kullanici 403, tarih sirasi, status her zaman `DRAFT`, sahte `createdById/status` yok sayilir; submit icin yetkili admin/system admin, yetkisiz roller, gecersiz status 409, transaction update+audit, update fail audit yok, audit fail rollback; review icin press/system admin karar verebilir, club admin veremez, yalniz `SUBMITTED` incelenir, bos red/degisiklik yorumu 400, update+review+audit transaction, update fail review/audit yok, audit/review fail rollback; publish icin press/system admin yayinlar, club admin 403, yalniz `APPROVED` yayinlanir, `publishedAt` set edilir, update+audit transaction, update fail audit yok, audit fail rollback; registration icin STUDENT rol kontrolu, yalniz gelecek `PUBLISHED`, duplicate 409, baslamis/dolu event 409, `capacity=null`, principal `userId`, son kontenjan yarisi, registration status yalniz current user; attendance token icin club admin/system admin yetkisi, token rotation, DB/audit icinde ham token yok, registered STUDENT check-in, kayitsiz/yanlis token/expired/window disi/duplicate check-in; attendance summary icin club admin/system admin yetkisi, status 409, sayimlar, absent/remaining/rate hesaplari ve veri minimizasyonu. Integration: public list auth yok 200, yalniz published gelecek eventler, internal alan yok, `from/to`, `clubId`, `q`, pagination, siralama, gecersiz query 400, public detail published 200, public olmayan/unknown 404, gecersiz id 400, registration auth yok 401, student 201, duplicate 409, non-student 403, public olmayan/unknown 404, baslamis/dolu 409, eszamanli son kontenjan kapasiteyi asmaz, registration status auth yok 401, student false/true, non-student 403, current-user-only, public olmayan 404; attendance token auth yok 401, club admin token 200, yetkisiz 403, student valid token check-in 201, duplicate 409, kayitsiz 403, wrong/expired/rotated token 400, eszamanli check-in count 1; attendance summary auth yok 401, club admin 200, baska admin/student/press 403, system admin 200, unknown 404, invalid id 400, draft/submitted 409, sifir/sinirsiz kapasite metrikleri ve internal alan yok; mevcut submit/review/publish route'lari calisir; create/submit/review/publish mevcut senaryolari korunur |
| `apps/api/src/clubs` | Unit ve Integration: club manageable endpoint yalniz admin/system_admin rolune sahip kulupleri listeler; club events endpoint kulupteki tum durumdaki etkinlikleri listeler, dogru yetki gerektirir (401, 403, 404). Yetkili kullanicilar icin verilerin (updatedAt vb.) dogru donmesi, internal sizinti olmamasi saglanir. |
| `apps/api/prisma/schema.prisma` | PostgreSQL calisirken `pnpm prisma:validate`, `pnpm prisma:generate`, `pnpm --filter @agu/api prisma:migrate:dev`, migration SQL etkisi incelemesi |
| `apps/api/prisma/seed.ts` | PostgreSQL calisirken `pnpm --filter @agu/api typecheck`, `pnpm --filter @agu/api seed`; seed ikinci kez de calistirilip idempotency kontrol edilir |
| `packages/contracts` | `pnpm --filter @agu/contracts lint`, `pnpm --filter @agu/contracts typecheck`, `pnpm --filter @agu/contracts test`, bagimli paketlerde typecheck |
| `packages/config` | `pnpm --filter @agu/config lint`, `pnpm --filter @agu/config typecheck`, bagimli paketlerde typecheck |
| `packages/ui` | `pnpm --filter @agu/ui lint`, `pnpm --filter @agu/ui typecheck`, `pnpm --filter @agu/ui build`, web build |
| Docs | Icerik incelemesi, gerekiyorsa Node 24 aktifken `pnpm lint` ile repo geneli kontrol |
| Health smoke | API: build ciktisindan `node apps/api/dist/src/main.js` ve `curl http://localhost:3001/health`; Web: `pnpm --filter @agu/web dev` ile API acik/kapali ana sayfa curl kontrolu |
| Auth smoke | API: seed kullaniciyla `POST /auth/dev-login`, cookie jar ile `GET /auth/me`, `POST /auth/logout`, ayni cookie jar ile `/auth/me` 401. Web: `NEXT_PUBLIC_ENABLE_DEV_AUTH=true pnpm --filter @agu/web dev` ile dev giris kontrolunun render edildigini ve API health sonucunu kontrol et |
| Events smoke | API build ciktisi calisirken club admin seed kullaniciyla dev-login, cookie ile `POST /events` 201 ve `DRAFT`; student seed kullaniciyla ayni payload 403 |
| Submit smoke | API build ciktisi calisirken gecici draft event olustur, club admin ile `POST /events/:eventId/submit` 200 ve `SUBMITTED`, ikinci submit 409, student ile baska draft submit 403, audit kaydi DB'de dogrulanir ve gecici kayitlar temizlenir |
| Review smoke | API build ciktisi calisirken uc gecici submitted event olustur, PRESS_EDITOR ile request-changes/reject/approve 200, statuslar ve `EventReview`/`AuditLog` kayitlari dogrulanir, karar verilmis evente ikinci karar 409, club admin karar girisimi 403, gecici kayitlar temizlenir |
| Publish smoke | API build ciktisi calisirken gecici approved event olustur, PRESS_EDITOR ile `POST /events/:eventId/publish` 200 ve `PUBLISHED`, `publishedAt` ve `AuditLog` dogrulanir, ikinci publish 409, club admin ile baska approved event publish 403, gecici kayitlar temizlenir |
| Public events smoke | API build ciktisi calisirken iki gelecek published, bir gecmis published, bir draft ve bir approved event olustur; `GET /events` default gelecek published listesi, `from/to`, `q`, pagination, public detail 200, public olmayan detail 404, internal alan yok ve publish POST route cakismasi kontrol edilir |
| Public web listing smoke | API ve web calisirken ana sayfa HTTP 200, published etkinlik kartlari, `q` ve tarih filtresi, pagination linkleri, bos sonuc, API kapaliyken hata durumu, development auth paneli development ortaminda var ve production build ciktisinda yok |
| Public web detail smoke | API ve web calisirken liste kartindan `/events/:eventId` linki, detail HTTP 200, public alan render'i, filtreli geri donus linki, public olmayan/unknown event icin guvenli 404, API kapaliyken 404 olmayan hata durumu, internal alan sizintisi yok ve metadata title/description kontrolu |
| Event registration smoke | API build ciktisi calisirken gelecek `PUBLISHED` ve `capacity=1` gecici event olustur, STUDENT ile ilk `POST /events/:eventId/register` 201, ayni kullanicinin ikinci istegi 409, baska STUDENT rolu olan kullanicinin istegi kapasite nedeniyle 409, DB registration count tam 1, gecici kayitlar temizlenir |
| Public web detail registration smoke | API ve web calisirken student dev-login, yayinlanmis gelecek etkinlik detayinda `Etkinliğe Katıl` gorunur, POST sonrasi `Bu etkinliğe kayıtlısınız` gorunur, sayfa yeniden acilinca kayit durumu korunur, student olmayan kullanicida buton gorunmez |
| QR attendance smoke | API build ciktisi calisirken gecici `PUBLISHED` event ve student registration olustur, club admin ile `POST /events/:eventId/attendance-token` 200, student ile `POST /events/:eventId/check-in` 201, ikinci check-in 409, token rotation sonrasi eski token 400, attendance count tam 1, gecici kayitlar temizlenir |
| Public web QR attendance panel smoke | API ve web calisirken club admin dev-login sonrasi kendi kulubunun published event detayinda `Yoklama Yönetimi` paneli gorunur, token uretimi 200, QR gorseli ve expiry bilgisi render edilir, yenileme sonrasi token degisir ve eski token check-in icin reddedilir, STUDENT ve PRESS_EDITOR icin panel render edilmez, HTML/log icinde ham token bulunmaz |
| Student QR check-in smoke | API ve web calisirken student dev-login, `/check-in` HTTP 200, kamera/manuel kontrol HTML'i, gecici published event+registration+attendance token ile manuel payload uzerinden check-in API 201, ikinci check-in 409, student olmayan kullanici icin API 403, HTML/log icinde ham token yok, gecici kayitlar temizlenir |
| Attendance summary smoke | API build ciktisi calisirken gecici published event icin 3 registration ve 2 attendance olustur, club admin ile `GET /events/:eventId/attendance-summary` 200 ve `3/2/1/66.7` metrikleri, baska kulup admini ve student icin 403, response icinde user/email/token yok, gecici kayitlar temizlenir |
| Public web attendance summary panel smoke | API ve web calisirken gecici published event detay sayfasi HTTP 200, club admin icin summary API `3/2/1/66.7` metrikleri, panel gorunurluk helper'i, baska kulup admini ve student icin API 403, sifir kayitli event metrikleri, response/HTML icinde ogrenci adi, email, userId veya token yok; hydrated refresh davranisi hafif web unit testleriyle dogrulanir |
| Club Dashboard smoke | API ve web calisirken club admin login olarak `/club-dashboard` hiti, 200, yetkili kulubun listede gorunmesi, dropdown icinde baska kuluplerin olmamasi, secili kulupten API ile etkinliklerin tum metriklerle kart/liste formatinda render edilmesi. Baska bir kullanici login olursa erisim sinirlari 403. |
